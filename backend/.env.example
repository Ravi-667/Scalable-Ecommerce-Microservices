PORT=5000
# Example (local docker-compose):
# MONGO_URI=mongodb://mongo:27017/se
# For production or Atlas, set your connection string in a local `.env` or in your host's env vars.
# DO NOT commit real credentials to the repository.
# Example Atlas (do not commit):
# mongodb+srv://<cluster0.xyz.mongodb.net/se?retryWrier>:<password>@ctes=true&w=majority
MONGO_URI=
JWT_SECRET=change_me
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_next_public_key
CLERK_SECRET_KEY=your_secret_key